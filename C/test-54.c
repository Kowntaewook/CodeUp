#include <stdio.h>
int num[5];
int max_number = 0;
int main(void)
{
    for(int i = 0; i < 5; i++)
        scanf("%d", num);
}
int max_number(void)
{
    for(int i = 0; i < 5; i++)
    {
        for(int j = i+1; j < 5; j++)
        {
            if(i < j)
            {
                
            }
        }
    }
}